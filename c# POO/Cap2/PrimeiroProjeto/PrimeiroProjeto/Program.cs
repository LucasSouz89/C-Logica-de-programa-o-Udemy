using System;

namespace PrimeiroProjeto {
    internal class Program {
        static void Main(string[] args) {

            // ---------------------------------------------------------
            // DICA DE OURO DO VISUAL STUDIO:
            // Pressione [Ctrl + K] e depois [Ctrl + D]
            // Isso arruma a INDENTAÇÃO (alinhamento) do código automaticamente.
            // ---------------------------------------------------------

            Console.WriteLine("Hello World!");
        }
    }
}